package org.whired.rskel.graphics;


/**
 * @author Whired
 */
public class WorldRenderer {

	// public static void renderTiles(Graphics graphics, BasicTile[][][] tiles) {
	// int xOffs = 0;
	// int yOffs = 0;
	// int largeWidth = 0;
	// for (int x = 0; x < tiles.length; x++) {
	// for (int y = 0; y < tiles[x].length; y++) {
	// for (int z = 0; z < tiles[x][y].length; z++) {
	// BasicTile t = tiles[x][y][z];
	// graphics.drawImage(t.getImage(), xOffs, yOffs, null);
	// yOffs += t.getHeight();
	// largeWidth = largeWidth < t.getWidth() ? t.getWidth() : largeWidth;
	// }
	// }
	// yOffs = 0;
	// xOffs += largeWidth;
	// }
	// }
}
